apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8dclusterlife.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "k8dclusterlife.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "k8dclusterlife.selectorLabels" . | nindent 4 }}
